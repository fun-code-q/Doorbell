<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+ backup rules. We forbid Google cloud backup AND device-to-device
     transfer of any sensitive storage, since it would otherwise carry the
     EncryptedSharedPreferences blob (and the Keystore handle is per-device,
     so transferring the prefs would leave them undecryptable anyway). -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="."/>
        <exclude domain="database"   path="."/>
        <exclude domain="file"       path="."/>
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="."/>
        <exclude domain="database"   path="."/>
        <exclude domain="file"       path="."/>
    </device-transfer>
</data-extraction-rules>
